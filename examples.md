# 对照

## 造词 vs 待确认

差：把 `job_type = 9` 写成「跨区域调度型订单」，或「跨派处理机制」。

好：项目里叫跨派订单就写跨派订单(`job_type = 9`)。没有中文名就只写 `job_type = 9`（待确认）。

## 过程句 vs 结论优先

差：

> 后端首先根据当前用户的权限信息判断其是否具备相关操作权限，在完成权限校验后，再结合订单当前状态以及相关业务数据进行综合判断，最终计算得出该字段的值……

好（字段 `can_sign`）：

**使用方法**：用于判断是否显示「签离」。用返回值决定显示或隐藏。

**传参方法**：本字段在响应里，布尔。`true` 显示，`false` 隐藏。

**注意事项**：不要自己用订单 `status` 猜。不要写「因为」。

## 一条一事

差：当订单处于已完成状态且属于普通订单，同时满足当前用户具有相关操作权限并且不存在未完成的关联任务时，系统允许签离。

好：调用方只看 `can_sign`。不要自己用订单状态拼条件。

这些条件若已是 **事实**（术语来源或合同写明），再拆成列表，一条一事。从 `if` 猜出来的列表不要写。

## 因果脑补

差：跨派订单由于涉及跨区域调度，因此不允许签离。

好：响应里有 `can_sign` 就写怎么用。`job_type = 9` 的中文名或原因不清楚就标 **待确认**，不要补「因为」。

## 程序流程 vs 调用路径

差：把 `resolveMany`、`array_merge`、写 verified 收成「一次点位解析」。这是从程序做摘要。

好：原料是页面和合同——未进工单贴 NFC 时调本接口；有 `data.point` 进工单节点页。这是 **调用路径**，不是程序流程图。

## 代码转中文 vs 三块对接

差：

> `nfcResolve` 会先按 `lbs_sop_nfc_tag.tag_code` 命中标签，再 `JobVisitState::resolveMany` 聚合 visit_status，服务中则内部调用 `nfcScan` 写 verified 并 `array_merge` 填写载荷。本质上这不是扫码，而是一次点位解析。

好：

**使用方法**：用于未进工单时贴 NFC 解析。
> 注意：`code≠200` 只 toast。有 `data.point` 进工单节点页；`point` 为 null 进工单页并 toast `tip`。

**传参方法**：NFC 码(`nfc_code`)，32 位小写 hex。

**注意事项**：工单内贴卡走扫码查看 SOP 详情(`nfcScan`)，不要用本接口。`is_inherited` 在 `data.point.risk_list[]` 里，不在根上。
