---
description: 给 App 的接口说明只用使用方法/传参方法/注意事项，词跟 CONTEXT.md
alwaysApply: false
---
# App 对接文档

改这些文件或同步 Apifox 说明时：

1. 每个接口按 **使用方法**、**传参方法**、**注意事项** 写。不要代码转中文。
2. 加粗领域词必须已在 `CONTEXT.md`。禁止新词。
3. 表名、PHP、落库不进这份。
4. 不要用 chinese-ai-humanizer。
5. 完整约定见 skill `app-api-docs`。
