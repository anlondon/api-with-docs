# API 对接说明写作

约束 Agent 如何写给调用方看的接口说明、Apifox 描述和字段说明。这里只放用词，不放模板细节。

## Language

**对接说明**:
给客户端调用方看的接口/字段文字，只回答怎么用。
_Avoid_: 概述, 实现原理, 数据流, 代码转中文

**业务术语**:
项目里已经在用的领域叫法。
_Avoid_: 为显得专业而新造的名字, 对同一概念的替换词

**术语来源**:
找 **业务术语** 的顺序：`CONTEXT.md`、当前接口已有说明、已有对接文档、Apifox 标题/说明、界面文案、代码注释。
_Avoid_: 根据实现「总结」出来的中文名

**待确认**:
尚未被资料证明的缺口：没有中文名，或没有已证实的含义/原因。留下 **代码标识** 或留空，标待确认。
_Avoid_: 自行起中文名, 为完整而补上的原因

**事实**:
**术语来源** 或接口合同里已经写明的内容。
_Avoid_: 只从 if 分支猜出来却写成确定句的含义

**推断**:
仅根据代码行为推测的业务含义。不进 **对接说明**。
_Avoid_: 写成确定的业务原因, 标了推断仍写进三块

**因果脑补**:
给代码行为补一个没有出处的「因为」。
_Avoid_: 因为涉及跨区域调度所以不允许签离

**代码标识**:
合同里会出现的接口名、字段名、枚举值。不是类名、表名、内部函数。
_Avoid_: 表名, SQL, 类名, 内部方法名

**中英对照**:
已有 **业务术语** 时，写成「中文(`代码标识`)」。接口、字段、相关接口都这样写。
_Avoid_: 只写英文函数名, 为成对而造的中文名

**用途**:
这接口或字段干什么，一句说完。
_Avoid_: 做单剧本, 先签字还不签离时调

**调用路径**:
调用方必须知道的约束：一次存几侧、成功看哪、失败怎么办、和哪个接口配合。原料是页面、接口合同、**术语来源**。
_Avoid_: 程序流程, Controller, Service, 内部函数顺序, 从 PHP 抽象出的业务总结

**程序流程**:
后端内部执行顺序。不进 **对接说明**。程序只用来核对字段是否真返回。
_Avoid_: 把实现凝练成业务段落

**结论优先**:
使用方法第一句写 **用途**，接着写必要的 **调用路径**。不是页面操作剧本。
_Avoid_: 先讲后端怎么算, 过程句, 第一句写做单时机

**参数对应**:
参数表里看不出来的：哪个枚举带哪张图、图从哪来、什么情况下多带字段、哪些不要传。
_Avoid_: 把必填、类型、字段清单再抄一遍

**参数表**:
Apifox 或文档里已有的请求/响应字段定义。
_Avoid_: 在传参方法正文里复述

**一条一事**:
一条业务规则只写一件允许或禁止。
_Avoid_: 一句话塞多个且/同时/并且条件

**流程状态**:
工单当前处于哪一档，例如未签到、暂停中、报告生成中。
_Avoid_: 用 toast 原文当状态名

**业务场景**:
哪类单、谁操作，例如跟进单、勘察单、主要技术员、协助技术员。
_Avoid_: 和流程状态混成一句

**状态分列**:
同一接口在不同 **流程状态** 或 **业务场景** 下能不能干不一样时，注意事项才按状态/场景拆开写。写法是「处于什么 / 哪种单 / 谁操作 → 能不能干什么」。
_Avoid_: 每条注意事项都套「工单处于」, 分列时每条再抄一句 toast

**易混接口**:
调用方容易拿来代替本接口的另一个接口，例如保存签名和签离。
_Avoid_: 没有对照对象却写「本接口不做某某」

**职责边界**:
有 **易混接口** 时必写：本接口不做什么，那件事走哪个接口。用 **中英对照**。
_Avoid_: 没有易混对象时的空边界句

**原文**:
正在改的那份接口说明、字段说明、Apifox 描述。
_Avoid_: 当不存在, 整篇作废重写

**加权标记**:
原文里标明必看、注意、小心、建议、前端必看等的段落，优先处理。
_Avoid_: 收成一句笼统用途, 不论好坏整条照抄

**原文质量**:
加权段落是否表达清晰、易读。好的保留句式；不好的优化写法，判断口径不能丢。
_Avoid_: 把清晰的判断句改成笼统用途

**收事实再重排**:
先过 **原文**，判断口径、字段含义、禁止旧字段、易混接口都留下，再排进三块。
_Avoid_: 覆盖式缩写

**覆盖式缩写**:
用三块空壳替换原文，删掉调用方用来判断界面的口径。
_Avoid_: 给列表点进去用

**过程句**:
把实现步骤翻译成中文的叙述。
_Avoid_: 首先根据权限再结合状态最终计算得出

**泛化词**:
用来包装业务、但不增加对接信息的空壳词：机制、体系、能力、链路、闭环、模型、引擎。
_Avoid_: 订单处理机制, 数据同步链路, 状态管理体系, 业务闭环, xxx引擎

## Relationships

- 写 **对接说明** 前先沿 **术语来源** 找 **业务术语**
- 找不到 **业务术语** 时，保留 **代码标识** 并标 **待确认**
- 有 **业务术语** 时用 **中英对照**，禁止只丢英文名
- **对接说明** 的使用方法：先 **用途**，再必要的 **调用路径**；用 **一条一事**；**过程句** 和 **程序流程** 不进正文
- **泛化词** 不得当新造包装；**术语来源** 里已有的照写，不替换、不扩写
- 业务仓库的 `CONTEXT.md` 只放词，不放产生条件、允许操作、限制；那些去业务流程文档
- 传参方法只写 **参数对应**，不抄 **参数表**
- 改已有说明时 **收事实再重排**，禁止 **覆盖式缩写**；**加权标记** 按 **原文质量** 保留句式或优化写法，排在注意事项前面
- **对接说明** 只写 **事实**，不写 **推断**；文档允许不完整指的是未知缺口，不是可以丢掉 **原文** 里已有的口径
- 多种 **流程状态** 或 **业务场景** 结果不同时，才做 **状态分列**；不是这种情况不要硬套「工单处于」
- 有 **易混接口** 时写 **职责边界**；没有对照对象不写空的「本接口不做」
- **因果脑补** 禁止；没有出处的「因为」不写
- 本仓库的 `CONTEXT.md` 不在写作时由本 skill 改写；业务仓库的 glossary 由 grill-with-docs 维护

## Example dialogue

> **Dev:** "没有 glossary，`job_type = 9` 我写成「跨区域调度型订单」吧，前端更好懂。"
> **Domain expert:** "不要。先看项目里是否已有 **业务术语**。没有就写 `job_type = 9` 并标 **待确认**。"
>
> **Dev:** "那我标上「推断：可能因为跨区域」总可以吧。"
> **Domain expert:** "不可以。**对接说明** 里零 **推断**。原因不知道就标 **待确认** 或别写。"
>
> **Dev:** "复杂业务我把程序流程凝练成一段业务说明，方便对接。"
> **Domain expert:** "凝练 **调用路径**，原料是合同和术语。不要把 **程序流程** 摘要成业务。"
>
> **Dev:** "使用方法写成『做单中先签字、还不签离时调』，这是调用路径。"
> **Domain expert:** "第一句写 **用途**（用于保存签名）。多种状态结果不同再 **状态分列**。不要写做单剧本。"
>
> **Dev:** "viewSOPPoint 三个字前端能看懂。"
> **Domain expert:** "有术语就写 **中英对照**：点击查看 SOP 详情(`viewSOPPoint`)。没有中文名才只留 **代码标识** 并标 **待确认**。"
>
> **Dev:** "job_id、data_type、sign_type 都写进传参，枚举写全。"
> **Domain expert:** "不抄 **参数表**。正文只写 **参数对应**：`sign_type` 客户带哪张图、技术员带哪张图。确收多带的字段进注意。"
>
> **Dev:** "注意事项每条都写成工单处于某某，并抄 toast。"
> **Domain expert:** "只有多种状态或场景结果不同才 **状态分列**。分列写能不能干，不抄 toast。单条边界不要硬套『工单处于』。"
>
> **Dev:** "每个接口都写一句本接口不做某某，显得完整。"
> **Domain expert:** "有 **易混接口** 才写 **职责边界**。保存签名旁边有签离，就要写不做签离、不应用他单。没有对照对象不要空写。"
>
> **Dev:** "原文太碎，我用三块重写，个性节点关闭那些判断精炼成『给列表点进去用』。"
> **Domain expert:** "那是 **覆盖式缩写**。先 **收事实再重排**。**加权标记** 按 **原文质量**：`can_disable=1` 且 `is_disabled=0` 这种清晰句留下；写糊的才改写法，口径不能丢。"

## Flagged ambiguities

- 「停下来不写」和「用代码标识兜底」——已决议：没有 glossary 时仍写 **对接说明**，用 **待确认**，不造词。
- 「加用途/参数/返回/业务规则栏目」和「保持三块」——已决议：栏目仍是三块；**用途** 是使用方法的第一句，不另开栏目。
- 使用方法第一句——已决议：写 **用途**，不是做单时机。必要的成功/失败/配合接口写在使用方法里 **用途** 之后。硬状态门槛进注意事项。
- 「业务对象/业务规则/操作/技术实现四类分类」——不进本 skill，留给蒸馏/业务梳理。
- **泛化词** ——已决议：禁止当新包装；项目里已有的照用。
- 「删除 30% 文字」——已决议：只删 **过程句**、套话、**泛化词**；字段表和枚举不砍。不做第二遍检查员，也不跑 chinese-ai-humanizer。
- 「业务规则写进 CONTEXT.md」——已决议：glossary 只定义词是什么；规则、测试用业务全图、「条件→动作→结果」不进本 skill。
- 对接要的先后顺序——已决议：在 **使用方法** 里写 **调用路径**；不画 **程序流程**；测试用业务全图仍在业务流程文档。
- 凝练原料——已决议：页面/接口合同/**术语来源**，不是后端执行步骤。
- **待确认** 从「缺中文名」扩展为「缺名称或缺已证实含义」；文档允许不完整。
- **推断** 是否进对接说明——已决议：不进。三块只写 **事实** 和 **待确认** 缺口。
- 接口/字段怎么写给测试和业务看——已决议：有 **业务术语** 就强制 **中英对照**；没有中文名不造词。
- 传参是否复述字段清单——已决议：不抄 **参数表**；只写 **参数对应**。类型和必填留给参数表。
- 注意事项是否一律改成「工单处于」并抄 toast——已决议：仅当需要按 **流程状态** / **业务场景** 分别说明时才 **状态分列**；分列不抄 toast；非分列不硬套句式。
- 「本接口不做某某」——已决议：有 **易混接口** 必写 **职责边界**；没有对照对象不空写。
- 改已有说明——已决议：**收事实再重排**，禁止 **覆盖式缩写**。**加权标记** 按 **原文质量**：清晰易读则留句式，不好则优化，口径必须还在。
