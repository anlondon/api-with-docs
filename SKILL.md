---
name: api-docs
description: >-
  Writes client-facing API docs and Apifox descriptions in 使用方法 / 传参方法 / 注意事项,
  using existing project terms or 代码标识+待确认, never coined names. When updating existing
  接口说明, harvest facts first (especially 必看/注意) and rearrange; do not overwrite with a
  shorter generic three-block. Use when updating 对接文档, Apifox endpoint/parameter/response
  descriptions. Do not use for general 去 AI 味 or copy polishing; do not run chinese-ai-humanizer
  on these docs.
disable-model-invocation: false
user-invocable: true
---

# API 对接说明

读者是客户端对接方（App / 前端 / 调用方）。只写怎么用。

文风像同事写对接说明：清楚、具体、可执行。不像报告，也不像闲聊。

用词见本仓库 [CONTEXT.md](CONTEXT.md)。本 skill **不**改业务仓库的 glossary，也不写业务流程文档。

## 术语

沿 **术语来源** 找 **业务术语**，找到就用，禁止换一种「更精炼」的说法。

没有 `CONTEXT.md`、也没跑 grill-with-docs 时，不要停写、也不要造词：

1. `CONTEXT.md`（有 `CONTEXT-MAP.md` 则先定位 context）
2. 已有对接文档、Apifox 标题/说明、界面文案、代码注释
3. 仍没有中文名 → 接口名或字段名原样写，标 **待确认**

有 **业务术语** 时强制 **中英对照**：保存签名(`saveSignatures`)、客户签名(`customer_signature_url`)。相关接口同样成对写。禁止只丢英文名，也禁止为了成对而造中文。

写作中发现新概念：标 **待确认**。不要一边写说明一边往 glossary 里塞。

## 改已有说明

有 **原文** 时必须先通读，再 **收事实再重排**。禁止 **覆盖式缩写**。

1. 扫 **加权标记**：必看、注意、小心、建议、前端必看。按 **原文质量** 处理：表达清晰、易读的保留句式（如何判断可以关闭：`can_disable=1` 且 `is_disabled=0`）；含糊、挤句、过程句的优化写法，判断口径不能丢。排进注意事项最前。不要收成一句笼统用途。
2. 再收：字段含义、禁止旧字段、相关接口、**参数对应**。
3. 排进三块，用 **中英对照**。参数表上的类型/必填仍不抄。
4. **原文** 没有的不要为完整而补。未知才标 **待确认**。

「文档允许不完整」只针对未知缺口，不是可以丢掉原文里已经写清的口径。

## 只写事实

三块里只放 **事实**（术语来源或接口合同已经写明的）。

- 不写 **推断**，也不标了推断往里塞
- 禁止 **因果脑补**：不要写没有出处的「因为」
- 内部 `if` 看到的行为，没有对应响应字段或已有说明，不要写进注意事项
- 名称或原因不知道：标 **待确认** 或留空。文档允许不完整，禁止为完整而补全

## 每个接口只写三块

项目另有对接模板时以项目为准；否则只用这三块。字段说明同样三块，写更短。

### 使用方法

**结论优先**：第一句写 **用途**（这接口干什么）。接着写必要的 **调用路径**：一次存几侧、成功看 `data` 还是回显、失败看报错、要配合哪个接口。

不要写做单剧本（「做单中先签字、还不签离时调」）。多种 **流程状态** 或 **业务场景** 下能不能干不一样，才在注意事项里 **状态分列**。

不要翻译 **程序流程**，也不要把内部实现「抽象成业务」。程序只用来核对字段是不是真返回。跨接口只写已证实的先后（先 A 后 B），不画 Controller/Service，不画测试用的业务全图。成功/失败/配合接口可以放在使用方法里 **用途** 后面的注意里，不要和第一句挤成一段。

### 传参方法

只写 **参数对应**，不抄 **参数表**。类型、是否必填、字段清单留给 Apifox 参数表。

正文写参数表看不出来的：哪个枚举对应哪块业务、带哪个字段、图从哪来。附加条件、禁止传的字段放在传参下面的注意。不要写「用法不变」，把非显而易见的配合拆开写。用 **中英对照**。

### 注意事项

**一条一事**。别和谁搞混、不要调哪些接口、不要用哪组字段自己猜业务。

只有同一接口在不同 **流程状态** 或 **业务场景** 下结果不同时，才 **状态分列**：工单处于未签到不能提交、跟进单不触发确收、协助技术员不触发确收。这时写「处于什么 / 哪种单 / 谁操作 → 能不能干什么」，不要每条再抄 toast。

不是这种分列，不要硬套「工单处于」。

有 **易混接口** 时必写 **职责边界**：本接口不做什么，那件事走哪个接口（保存签名不做签离、不把签名应用到其他单）。没有易混对象，不要空写「本接口不做某某」。

多个已证实的条件拆成列表，禁止写成「当……且……同时……」。条件本身不是 **事实** 就不要列。

Apifox 的接口说明、参数说明、响应说明同一套三块，不要另起「概述 / 实现原理 / 数据流」。怎么调 Apifox CLI 走 `apifox-cli`。

对照见 [examples.md](examples.md)。

## 禁止

- 造词；为避免重复而换同义词；用 **泛化词** 包装具体业务（机制、体系、能力、链路、闭环、模型、引擎）。项目里已经这么叫的，照写
- **覆盖式缩写**：有原文却整篇换成笼统三块
- **过程句**：把实现翻译成中文长段
- 表名、SQL、落库、队列、类名、内部函数。合同里的 **代码标识**（接口名、字段名、枚举值）可以写
- 同一含义两个字段名并列当主说明
- 跑 `/chinese-ai-humanizer`，或写完再开一轮「反 AI 检查员」

## 去 AI 味（写的时候就遵守）

- 不用「本质上 / 实际上 / 值得注意的是 / 首先其次最后 / 综上所述 / 一句话讲清楚」
- 不用「不是……而是……」、排比、金句、比喻、`——`
- 中文用中文标点
- 不把实现讲成原理散文；套话删掉
- 不要口语到省略约定；也不要写成论文或公众号

## 写完自检

只删 **过程句**、套话、**泛化词**。不抄进正文的 **参数表** 本来就不在三块里。

- 有没有 **术语来源** 里没有、又没标 **待确认** 的新词？
- 有没有表名 / 类名 / 内部函数 / **过程句** / **程序流程** / **推断** / 没有出处的「因为」？
- 有没有该成对却只写了英文、或为成对造了新中文？
- 有没有把 **参数表** 抄进传参？
- 有 **易混接口** 却没写 **职责边界**？没有对照对象却空写了「本接口不做」？
- 有 **原文** 时有没有 **覆盖式缩写**？**加权标记** 的判断口径还在不在？清晰句有没有被改糊，糊句有没有该优化却没动？
- 该 **状态分列** 的却挤成一句，或不该分列却条条「工单处于」？
- 有没有报告腔、换词、金句、一句话里多条规则、为完整而补上的内容？
